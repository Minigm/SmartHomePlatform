<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gm.smartHomePlatform">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Administrator.act_admin_device"></activity>
        <activity android:name=".Main.act_company_main" />
        <activity android:name=".Administrator.act_admin_main" />
        <activity android:name=".Main.act_user_main" />
        <activity android:name=".Login.act_forget_password" />
        <activity android:name=".Login.act_add_user" />
        <activity android:name=".Login.act_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>